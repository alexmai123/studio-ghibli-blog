<!DOCTYPE html>
<html lang="en">
<head>
  <title>Favourite</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
<body style="padding:100px;">

  <div class="container">
  <div>
  	<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Studio Ghibli Gallery</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active" id="home"><a href="/">Home <span class="sr-only">(current)</span></a></li>
      </ul>  
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
</div>
<div>
<h2> Favourite
</h2>
</div>
<div>
	 <table class="table table-bordered table-striped">
    <tr>
          <td>Title</td>
          <td>Director</td>
          <td></td>
    </tr>
    <% for(var key in fav[0].fv) {%>
 		   <tr>
          <td><%= fav[0].fv[key].title %></td>
          <td><%= fav[0].fv[key].director %></td>
          <td>
            <form action="/favourite/<%= key %>" method="POST">
              <button class="btn btn-default">Delete</button>
            </form>
          </td> 
        </tr>

    <% } %>
    </table>
</div>
</div>
</body>
